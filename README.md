# Микросервисная архитектура

Проект состоит из 6 микросервисов, работающих на Go и взаимодействующих через HTTP API.

> **Последнее обновление:** Автоматический деплой через CI/CD настроен и готов к работе.

## 📦 Микросервисы

- **auth-service** (порт 8080) - Аутентификация и авторизация
- **user-service** (порт 8085) - Управление пользователями
- **product-service** (порт 8081) - Управление продуктами
- **project-service** (порт 8082) - Управление проектами
- **portfolio-service** (порт 8083) - Управление портфолио
- **contact-service** (порт 8084) - Управление контактами

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Запуск всех сервисов
make dev

# Или через скрипт
./scripts/dev.sh

# Запуск отдельного сервиса
make dev-auth
make dev-product
# и т.д.
```

### Деплой через Docker Compose

```bash
# Запуск всех сервисов в Docker
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка всех сервисов
docker-compose down

# Пересборка и запуск
docker-compose up -d --build
```

### Деплой через скрипт

```bash
# Деплой на production
./scripts/deploy.sh production

# Деплой на staging
./scripts/deploy.sh staging
```

## 🔧 Настройка переменных окружения

1. Скопируйте `env.example` в `.env`:
```bash
cp env.example .env
```

2. Отредактируйте `.env` и укажите необходимые значения:
- `JWT_SECRET` - секретный ключ для JWT токенов
- `AUTH_SERVICE_URL` - URL auth-service (для других сервисов)
- Пути к базам данных для каждого сервиса

## 🐳 Docker

### Сборка отдельных сервисов

```bash
# Auth Service
cd auth-service
docker build -t auth-service:latest .

# Product Service
cd product-service
docker build -t product-service:latest .

# И так далее для остальных сервисов
```

### Запуск через Docker Compose

Все сервисы настроены в `docker-compose.yml` в корне проекта:

```bash
docker-compose up -d
```

Сервисы будут доступны на:
- http://localhost:8080 - auth-service
- http://localhost:8081 - product-service
- http://localhost:8082 - project-service
- http://localhost:8083 - portfolio-service
- http://localhost:8084 - contact-service
- http://localhost:8085 - user-service

## 🔄 CI/CD Pipeline

Проект использует GitHub Actions для автоматизации CI/CD.

### Конфигурация

CI/CD pipeline настроен в `.github/workflows/ci-cd.yml`:

- **Ветка `dev`**: При пуше запускаются только тесты
- **Ветка `master`**: При пуше запускаются тесты, сборка Docker образов и деплой на сервер

### Настройка секретов в GitHub

Для работы CI/CD необходимо настроить следующие секреты в GitHub (Settings → Secrets and variables → Actions):

- `DEPLOY_HOST` - IP адрес или домен сервера для деплоя (например: `192.168.1.100` или `example.com`)
- `DEPLOY_USER` - пользователь для SSH подключения (например: `root` или `deploy`)
- `DEPLOY_SSH_KEY` - **приватный** SSH ключ для подключения к серверу (см. инструкцию ниже)
- `DEPLOY_SSH_PASSPHRASE` - пароль от SSH ключа (если ключ защищен паролем, оставьте пустым если нет)
- `DEPLOY_PATH` - путь на сервере, куда будет развернут проект (например: `/opt/microservices`)

#### ⚠️ Важно: Настройка SSH ключей

**Проблема:** Если вы видите ошибку `ssh: no key found`, значит в GitHub Secrets добавлен публичный ключ вместо приватного.

**Решение:**

1. **Найдите приватный SSH ключ на вашем компьютере:**
   ```bash
   # Обычно находится в ~/.ssh/id_rsa или ~/.ssh/id_ed25519
   cat ~/.ssh/id_rsa
   # или
   cat ~/.ssh/id_ed25519
   ```

2. **Скопируйте ВЕСЬ приватный ключ** (он начинается с `-----BEGIN OPENSSH PRIVATE KEY-----` или `-----BEGIN RSA PRIVATE KEY-----` и заканчивается `-----END ... KEY-----`)

3. **Добавьте приватный ключ в GitHub Secrets:**
   - Перейдите в Settings → Secrets and variables → Actions
   - Создайте или обновите секрет `DEPLOY_SSH_KEY`
   - Вставьте **весь** приватный ключ (включая строки `-----BEGIN` и `-----END`)

4. **Добавьте публичный ключ на сервер:**
   ```bash
   # На сервере выполните:
   mkdir -p ~/.ssh
   chmod 700 ~/.ssh
   
   # Добавьте публичный ключ в authorized_keys
   echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIESj5fHpsGmFGtYTI++Y2PZ8Z6jq2CvCi2hUEiSob7nM alexstroilov2007@icloud.com" >> ~/.ssh/authorized_keys
   chmod 600 ~/.ssh/authorized_keys
   ```

5. **Проверьте подключение:**
   ```bash
   ssh -i ~/.ssh/id_rsa user@your-server-ip
   ```

### Локальный запуск тестов

```bash
# Тесты для auth-service
cd auth-service
go test ./...

# Тесты для product-service
cd product-service
go test ./...

# И так далее для остальных сервисов
```

## 📋 Деплой на сервер

### Подготовка сервера

1. Установите Docker и Docker Compose на сервер:
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install docker.io docker-compose -y
sudo systemctl start docker
sudo systemctl enable docker
```

2. Создайте директорию для проекта:
```bash
sudo mkdir -p /opt/microservices
sudo chown $USER:$USER /opt/microservices
```

3. Клонируйте репозиторий:
```bash
cd /opt/microservices
git clone <your-repo-url> .
```

4. Создайте `.env` файл:
```bash
cp env.example .env
nano .env  # Отредактируйте значения
```

### Ручной деплой

```bash
# На сервере
cd /opt/microservices
git pull origin master
docker-compose down
docker-compose build
docker-compose up -d
docker-compose ps
```

### Автоматический деплой через CI/CD

При пуше в ветку `master` GitHub Actions автоматически:
1. Запустит тесты
2. Соберет Docker образы для всех сервисов
3. Подключится к серверу по SSH
4. Обновит код, пересоберет и перезапустит контейнеры

## 🔍 Мониторинг и логи

### Просмотр логов

```bash
# Логи всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f auth-service
docker-compose logs -f product-service
```

### Проверка статуса

```bash
# Статус всех контейнеров
docker-compose ps

# Health checks
docker-compose ps --format "table {{.Name}}\t{{.Status}}"
```

## 🛠️ Разработка

### Структура проекта

```
.
├── auth-service/          # Сервис аутентификации
├── user-service/          # Сервис пользователей
├── product-service/       # Сервис продуктов
├── project-service/       # Сервис проектов
├── portfolio-service/     # Сервис портфолио
├── contact-service/       # Сервис контактов
├── docker-compose.yml     # Конфигурация для деплоя
├── .github/
│   └── workflows/
│       └── ci-cd.yml      # CI/CD pipeline
└── scripts/
    ├── dev.sh            # Скрипт для локальной разработки
    └── deploy.sh         # Скрипт для деплоя
```

### Добавление нового сервиса

1. Создайте директорию для сервиса
2. Добавьте `Dockerfile`
3. Добавьте сервис в `docker-compose.yml`
4. Обновите CI/CD pipeline в `.github/workflows/ci-cd.yml`

## 📚 Дополнительная документация

- [Взаимодействие микросервисов](./MICROSERVICES_INTERACTION.md) - подробная документация по взаимодействию между сервисами

## 🐛 Устранение неполадок

### Проблемы с Docker

```bash
# Очистка всех контейнеров и образов
docker-compose down -v
docker system prune -a

# Пересборка с нуля
docker-compose build --no-cache
docker-compose up -d
```

### Проблемы с портами

Если порты заняты, измените маппинг портов в `docker-compose.yml`:

```yaml
ports:
  - "8080:8080"  # Измените первый порт на свободный
```

### Проблемы с базой данных

Базы данных хранятся в `./<service-name>/data/`. Убедитесь, что директории существуют:

```bash
mkdir -p auth-service/data
mkdir -p product-service/data
# и т.д.
```

## 📝 Лицензия

[Укажите лицензию проекта]

## 👥 Авторы

[Укажите авторов проекта]

