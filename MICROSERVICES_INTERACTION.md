# Взаимодействие микросервисов

Документация по взаимодействию между микросервисами в системе.

## 📊 Схема взаимодействия

```
┌─────────────────────────────────────────────────────────────────┐
│                         Клиент (Frontend)                        │
└────────────────────────────┬──────────────────────────────────────┘
                             │
                             │ HTTP Request с JWT токеном
                             │ Authorization: Bearer <token>
                             ▼
        ┌────────────────────────────────────────────────────┐
        │         Любой микросервис (8081-8085)              │
        │  ┌──────────────────────────────────────────────┐  │
        │  │ 1. Middleware проверяет JWT токен           │  │
        │  │    - Парсит токен                            │  │
        │  │    - Извлекает userID из claims              │  │
        │  └──────────────┬───────────────────────────────┘  │
        │                 │                                   │
        │                 │ HTTP GET /api/v1/internal/       │
        │                 │ users/:id/role                   │
        │                 ▼                                   │
        │  ┌──────────────────────────────────────────────┐  │
        │  │ 2. AuthClient вызывает Auth Service          │  │
        │  │    - Получает роль пользователя              │  │
        │  │    - Сохраняет в контекст (isAdmin)          │  │
        │  └──────────────┬───────────────────────────────┘  │
        │                 │                                   │
        │                 │ HTTP Response                     │
        │                 │ { "id": 1, "role": "admin" }      │
        │                 ▼                                   │
        │  ┌──────────────────────────────────────────────┐  │
        │  │ 3. AdminMiddleware проверяет роль            │  │
        │  │    - Если admin → разрешает доступ          │  │
        │  │    - Если user → возвращает 403 Forbidden   │  │
        │  └──────────────┬───────────────────────────────┘  │
        │                 │                                   │
        │                 ▼                                   │
        │  ┌──────────────────────────────────────────────┐  │
        │  │ 4. Handler обрабатывает запрос             │  │
        │  │    - Выполняет бизнес-логику                │  │
        │  │    - Возвращает результат                   │  │
        │  └─────────────────────────────────────────────┘  │
        └────────────────────────────────────────────────────┘
                             │
                             │ HTTP Response
                             ▼
                    ┌─────────────────┐
                    │  Auth Service   │
                    │   (Port 8080)   │
                    └─────────────────┘
```

---

## 🔄 Типы взаимодействия

### 1. Аутентификация и авторизация

**Все сервисы (кроме Auth Service) используют Auth Service для:**
- Проверки JWT токенов
- Получения ролей пользователей
- Валидации прав доступа

#### Пример потока:

```
1. Клиент → Product Service
   POST /api/products
   Headers: Authorization: Bearer <JWT_TOKEN>

2. Product Service → Auth Service
   GET /api/v1/internal/users/1/role
   (внутренний запрос, без токена)

3. Auth Service → Product Service
   Response: { "id": 1, "role": "admin" }

4. Product Service проверяет роль
   - Если admin → обрабатывает запрос
   - Если user → возвращает 403 Forbidden
```

---

## 🔌 Внутренние API Auth Service

Auth Service предоставляет внутренние эндпоинты для других сервисов:

### 1. Получить пользователя по ID

**Эндпоинт:** `GET /api/v1/internal/users/:id`

**Используется в:**
- User Service (для получения данных пользователя)

**Пример запроса:**
```go
// User Service
authClient := clients.NewAuthClient()
user, err := authClient.GetUser(userID)
```

**HTTP запрос:**
```http
GET http://localhost:8080/api/v1/internal/users/1
```

**Ответ:**
```json
{
  "data": {
    "id": 1,
    "email": "user@example.com",
    "full_name": "John Doe",
    "role": "admin",
    "created_at": "2024-01-01T00:00:00Z",
    "updated_at": "2024-01-01T00:00:00Z"
  }
}
```

---

### 2. Получить роль пользователя

**Эндпоинт:** `GET /api/v1/internal/users/:id/role`

**Используется в:**
- Product Service (middleware)
- Project Service (middleware)
- Portfolio Service (middleware)
- Contact Service (middleware)
- User Service (middleware)

**Пример запроса:**
```go
// Product Service Middleware
authClient := clients.NewAuthClient()
role, err := authClient.GetUserRole(userID)
if role == "admin" {
    c.Set("isAdmin", true)
}
```

**HTTP запрос:**
```http
GET http://localhost:8080/api/v1/internal/users/1/role
```

**Ответ:**
```json
{
  "data": {
    "id": 1,
    "role": "admin"
  }
}
```

---

### 3. Обновить роль пользователя

**Эндпоинт:** `PATCH /api/v1/internal/users/:id/role`

**Используется в:**
- User Service (для изменения ролей)

**Пример запроса:**
```go
// User Service
authClient := clients.NewAuthClient()
err := authClient.UpdateUserRole(userID, "admin")
```

**HTTP запрос:**
```http
PATCH http://localhost:8080/api/v1/internal/users/1/role
Content-Type: application/json

{
  "role": "admin"
}
```

**Ответ:**
```json
{
  "data": {
    "id": 1,
    "email": "user@example.com",
    "full_name": "John Doe",
    "role": "admin",
    ...
  }
}
```

---

### 4. Список пользователей

**Эндпоинт:** `GET /api/v1/users?page=1&size=20&role=user&email=...`

**Используется в:**
- User Service (для получения списка пользователей)

**Пример запроса:**
```go
// User Service
authClient := clients.NewAuthClient()
filters := map[string]string{
    "role": "user",
    "email": "user@example.com",
}
users, total, err := authClient.ListUsers(1, 20, filters)
```

**HTTP запрос:**
```http
GET http://localhost:8080/api/v1/users?page=1&size=20&role=user&email=user@example.com
Authorization: Bearer <ADMIN_TOKEN>
```

**Ответ:**
```json
{
  "data": {
    "items": [
      {
        "id": 1,
        "email": "user@example.com",
        "full_name": "John Doe",
        "role": "user"
      }
    ],
    "total": 1,
    "page": 1,
    "size": 20
  }
}
```

---

## 🔐 Процесс аутентификации

### Шаг 1: Пользователь логинится

```
Клиент → Auth Service
POST /api/v1/login
{
  "email": "user@example.com",
  "password": "password123"
}

Auth Service → Клиент
{
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

### Шаг 2: Клиент использует токен

```
Клиент → Product Service
POST /api/products
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
{
  "title": "Product Name",
  "price": 99.99
}
```

### Шаг 3: Product Service проверяет токен

```go
// Product Service Middleware
1. Извлекает токен из заголовка Authorization
2. Парсит JWT токен с помощью JWT_SECRET
3. Извлекает userID из claims (sub или user_id)
4. Вызывает Auth Service для получения роли
```

### Шаг 4: Product Service получает роль

```
Product Service → Auth Service
GET /api/v1/internal/users/1/role

Auth Service → Product Service
{
  "data": {
    "id": 1,
    "role": "admin"
  }
}
```

### Шаг 5: Product Service проверяет права

```go
// Product Service AdminMiddleware
if role == "admin" {
    // Разрешает доступ
    c.Next()
} else {
    // Отклоняет запрос
    c.AbortWithStatusJSON(403, {"error": "admin only"})
}
```

---

## 📝 Примеры кода

### AuthClient (используется во всех сервисах)

```go
// clients/auth_client.go
type AuthClient struct {
    BaseURL string
}

func NewAuthClient() *AuthClient {
    baseURL := os.Getenv("AUTH_SERVICE_URL")
    if baseURL == "" {
        baseURL = "http://localhost:8080"
    }
    return &AuthClient{BaseURL: baseURL}
}

func (c *AuthClient) GetUserRole(userID uint) (string, error) {
    url := fmt.Sprintf("%s/api/v1/internal/users/%d/role", c.BaseURL, userID)
    resp, err := http.Get(url)
    // ... обработка ответа
    return role, nil
}
```

### Middleware (используется во всех сервисах)

```go
// middleware/auth.go
func AuthMiddleware() gin.HandlerFunc {
    return func(c *gin.Context) {
        // 1. Извлекает токен
        tokenStr := strings.TrimPrefix(auth, "Bearer ")
        
        // 2. Парсит JWT
        token, err := jwt.Parse(tokenStr, func(t *jwt.Token) (interface{}, error) {
            return []byte(cfg.JWTSecret), nil
        })
        
        // 3. Извлекает userID
        userID := uint(claims["sub"].(float64))
        c.Set("userID", userID)
        
        // 4. Получает роль из Auth Service
        authClient := clients.NewAuthClient()
        role, err := authClient.GetUserRole(userID)
        c.Set("isAdmin", role == "admin")
        
        c.Next()
    }
}
```

### Использование в Handler

```go
// handlers/products.go
func CreateProduct(c *gin.Context) {
    // userID уже установлен middleware
    userID := c.GetUint("userID")
    
    // Создаем продукт
    product := models.Product{...}
    db.DB.Create(&product)
    
    // Логируем действие
    logs.SendLog(userID, "created product id=" + strconv.Itoa(int(product.ID)))
    
    c.JSON(201, product)
}
```

---

## 🔄 Потоки данных

### Поток 1: Создание продукта (Admin)

```
1. Клиент → Product Service
   POST /api/products
   Authorization: Bearer <ADMIN_TOKEN>
   Body: { "title": "Product", "price": 99.99 }

2. Product Service Middleware:
   - Проверяет JWT токен
   - Извлекает userID = 1
   - Вызывает Auth Service: GET /api/v1/internal/users/1/role
   - Получает role = "admin"
   - Устанавливает isAdmin = true

3. Product Service AdminMiddleware:
   - Проверяет isAdmin = true
   - Разрешает доступ

4. Product Service Handler:
   - Создает продукт в БД
   - Логирует действие с userID = 1
   - Возвращает созданный продукт

5. Product Service → Клиент
   Response: 201 Created
   Body: { "id": 1, "title": "Product", "price": 99.99 }
```

### Поток 2: Изменение роли пользователя (User Service)

```
1. Клиент → User Service
   PATCH /api/users/2/role
   Authorization: Bearer <ADMIN_TOKEN>
   Body: { "role": "admin" }

2. User Service Middleware:
   - Проверяет JWT токен
   - Получает роль текущего пользователя = "admin"
   - Разрешает доступ

3. User Service Handler:
   - Вызывает Auth Service: GET /api/v1/internal/users/2
   - Получает данные пользователя
   - Вызывает Auth Service: PATCH /api/v1/internal/users/2/role
   - Обновляет роль в Auth Service
   - Логирует действие в своей БД
   - Возвращает результат

4. User Service → Клиент
   Response: 200 OK
   Body: { "id": 2, "role": "admin" }
```

### Поток 3: Просмотр портфолио (публичный)

```
1. Клиент → Portfolio Service
   GET /api/portfolio
   (без токена)

2. Portfolio Service:
   - Эндпоинт публичный, middleware не применяется
   - Возвращает список портфолио

3. Portfolio Service → Клиент
   Response: 200 OK
   Body: [{ "id": 1, "title": "Portfolio 1", ... }]
```

---

## 🛠️ Технические детали

### Переменные окружения

Все сервисы (кроме Auth Service) должны иметь:

```bash
JWT_SECRET=your-secret-key-here  # Для проверки JWT токенов
AUTH_SERVICE_URL=http://localhost:8080  # URL Auth Service
```

### Формат JWT токена

Токен содержит:
```json
{
  "sub": 1,  // userID
  "email": "user@example.com",
  "exp": 1234567890,
  "iat": 1234567890
}
```

### Обработка ошибок

Если Auth Service недоступен:
- Middleware использует fallback на claims токена
- Если в токене есть `is_admin`, используется он
- Если нет, пользователь считается обычным пользователем

### Внутренние запросы

Внутренние эндпоинты Auth Service (`/api/v1/internal/*`) **не требуют аутентификации** и предназначены только для межсервисного взаимодействия.

---

## 📊 Сводная таблица взаимодействий

| Сервис | Вызывает Auth Service | Методы | Назначение |
|--------|----------------------|--------|------------|
| **Product Service** | ✅ | `GetUserRole()` | Проверка роли для админских операций |
| **Project Service** | ✅ | `GetUserRole()` | Проверка роли для админских операций |
| **Portfolio Service** | ✅ | `GetUserRole()` | Проверка роли для админских операций |
| **Contact Service** | ✅ | `GetUserRole()` | Проверка роли для админских операций |
| **User Service** | ✅ | `GetUser()`, `GetUserRole()`, `ListUsers()`, `UpdateUserRole()` | Получение и управление пользователями |

---

## 🔍 Отладка взаимодействий

### Проверка связи с Auth Service

```bash
# Проверить, что Auth Service доступен
curl http://localhost:8080/api/v1/internal/users/1/role

# Проверить получение роли пользователя
curl http://localhost:8080/api/v1/internal/users/1/role
```

### Логирование запросов

Все сервисы логируют взаимодействия:
- Успешные запросы к Auth Service
- Ошибки при недоступности Auth Service
- Fallback на claims токена

### Мониторинг

Рекомендуется отслеживать:
- Количество запросов к Auth Service от каждого сервиса
- Время ответа Auth Service
- Процент ошибок при взаимодействии

---

## 🚀 Рекомендации

1. **Кэширование ролей** - можно кэшировать роли пользователей на короткое время
2. **Retry механизм** - добавить повторные попытки при ошибках Auth Service
3. **Circuit Breaker** - защита от каскадных отказов
4. **Health checks** - проверка доступности Auth Service перед запросами
5. **Метрики** - отслеживание времени ответа и ошибок

---

**Примечание:** Все взаимодействия между сервисами происходят через HTTP REST API. В будущем можно рассмотреть использование gRPC для улучшения производительности.



